---
- hosts: master
  tasks:
    - name: Run the equivalent of "apt-get update"
      apt:
        update_cache: yes
      become: true
      become_user: root

    - name: Install Mosquitto (Client)
      apt:
        name: mosquitto-clients
      become: true
      become_user: root