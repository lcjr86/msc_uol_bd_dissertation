---
- hosts: master
  tasks:
    - name: Copy '/opt/hadoop/' files to the slave01
      synchronize:
        #src: /opt/hadoop/
        #dest: lcjr86@slave01:/opt/hadoop/
        src: /opt/hadoop
        dest: root@slave01:/opt/hadoop
        #dest: lcjr86@slave01:/opt/hadoop/
      become: true
      become_user: root

  tasks:
    - name: Copy '/opt/hadoop/' files to the slave02
      synchronize:
        #src: /opt/hadoop/
        #dest: lcjr86@slave01:/opt/hadoop/
        src: /opt/hadoop
        dest: root@slave02:/opt/hadoop
        #dest: lcjr86@slave01:/opt/hadoop/
      become: true
      become_user: root

  tasks:
    - name: Copy '/opt/hadoop/' files to the slave03
      synchronize:
        #src: /opt/hadoop/
        #dest: lcjr86@slave01:/opt/hadoop/
        src: /opt/hadoop
        dest: root@slave03:/opt/hadoop
        #dest: lcjr86@slave01:/opt/hadoop/
      become: true
      become_user: root

#-a, --archive               archive mode
#-v, --verbose               increase verbosity
#-x, --one-file-system       don't cross filesystem boundaries
#-P                          same as --partial --progress
